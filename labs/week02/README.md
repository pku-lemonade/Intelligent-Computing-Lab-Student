# Week 02 样例文件

课程资料包已经提供 `week02-sample.nsys-rep` 和指定 kernel 的 Compute 证据。下载后用 Nsight Systems GUI 打开 Systems 报告，再按指导书阅读 `.ncu-rep`、导出表或截图。样例只用于练习读图，报告中必须标注“课程采集”，不能改写成个人 GPU 实测。

`analysis-sheet.md` 是空白记录表，`sample_workload.py` 只用于课程资料更新，不需要学生拥有模型或 GPU。
